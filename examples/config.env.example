# =============================================================================
# STM32 MCP Documentation Server - Minimal Configuration Example
# =============================================================================
# Copy this file to .env in your project root and modify as needed.
#
# Usage:
#   cp examples/config.env.example .env
#   # Edit settings as needed
#   stm32-docs  # Start the server
# =============================================================================

# Server Mode: local (stdio), network (HTTP/SSE), or hybrid (both)
STM32_SERVER_MODE=local

# Network settings (only used in network/hybrid mode)
STM32_HOST=0.0.0.0
STM32_PORT=8765

# Embedding model - affects quality vs speed tradeoff
# Options: all-MiniLM-L6-v2 (fast), all-MiniLM-L12-v2 (better), all-mpnet-base-v2 (best)
STM32_EMBEDDING_MODEL=all-MiniLM-L6-v2

# Logging level: DEBUG, INFO, WARNING, ERROR
STM32_LOG_LEVEL=INFO

# Search settings
STM32_DEFAULT_SEARCH_RESULTS=5
STM32_MAX_SEARCH_RESULTS=20
STM32_MIN_RELEVANCE_SCORE=0.3

# Chunking settings (for document ingestion)
STM32_CHUNK_SIZE=1000
STM32_CHUNK_OVERLAP=150
